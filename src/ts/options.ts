import { Options } from "./interfaces";

const generatorOptions: Options = {
  length: "8",
  uppercase: false,
  lowercase: false,
  numbers: false,
  symbols: false,
};

export { generatorOptions };
